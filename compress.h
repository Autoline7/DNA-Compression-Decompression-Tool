#ifndef COMPRESS_H
#define COMPRESS_H

//the main funtion that compresses the method
void compress(char[]);

//prints the answer takes in an interger array and length of the array
void printAns(int [], int length);

//returns the int value of the given character
int valueOf(char);
#endif
